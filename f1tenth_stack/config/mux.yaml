ackermann_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : drive
        timeout : 0.2
        priority: 10
      joystick:
        topic   : teleop
        timeout : 0.2
        priority: 100
    locks:
      auto_disabled:
        topic   : auto_drive_disabled
        timeout : 0.3
        priority: 50
